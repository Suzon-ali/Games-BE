const chatSearchableFields = ['userName'];
const excludeFields = ['sortBy', 'sortOrder', 'sort', 'search', 'limit', 'page', 'fields'];

export const chatSearchFields = {
  chatSearchableFields,
  excludeFields,
};
